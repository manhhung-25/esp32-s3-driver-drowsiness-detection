# Python 3.10-3.12 is recommended for the optional model and video tools.
# The ESP32 firmware build uses ESP-IDF's own Python environment; these
# packages are not required when you only build/flash the committed models.
numpy>=1.24,<2
opencv-python>=4.8,<5
onnx>=1.14,<2
onnxruntime>=1.16,<2
torch>=2.1,<3
torchvision>=0.16,<1
esp-ppq>=1.0
